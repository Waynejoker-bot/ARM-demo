---
description: 测试驱动开发（TDD）- 先写测试，后写代码，无例外
alwaysApply: true
---

# 测试驱动开发（TDD）

## 铁律

```
没有先写失败的测试，就不能写任何生产代码
```

先写了代码？**删掉，重新开始。** 不能保留作"参考"，不能"边改边写测试"。删除就是删除。

## Red-Green-Refactor 循环

1. **RED** — 写一个最小的失败测试，描述期望行为
2. **验证 RED** — 运行测试，**必须看到它失败**，且失败原因正确（功能不存在，不是语法错误）
3. **GREEN** — 写最少的代码让测试通过（不要过度实现）
4. **验证 GREEN** — 运行所有测试，确认全部通过
5. **REFACTOR** — 清理代码，保持测试绿色
6. 重复

## 好测试的标准

- 名称描述行为，不描述实现（`test('空邮箱被拒绝')` ✅，`test('test1')` ❌）
- 测试真实代码，尽量避免 mock
- 一个测试只测一件事（名称含"和"→ 拆分）

## 必须使用 TDD 的场景

新功能 / Bug 修复 / 重构 / 行为变更 — **无一例外**

## 常见借口 = 停下来，重新开始

| 借口 | 现实 |
|------|------|
| "太简单了不需要测试" | 简单代码也会出 bug，写测试只需 30 秒 |
| "我先实现，后面再补测试" | 后写的测试会立即通过，证明不了任何事 |
| "我已经手动测试过了" | 手动测试无法重现，无法在代码变更后重跑 |
| "删掉 X 小时的代码太浪费" | 沉没成本谬误。保留不可信的代码才是真正的浪费 |
