---
description: 系统化调试 - 找到根本原因再修复，禁止猜测式修复
alwaysApply: true
---

# 系统化调试

## 铁律

```
未完成根本原因调查，不得提出任何修复方案
```

## 四个阶段（必须按顺序完成）

### Phase 1：根本原因调查
- 仔细阅读完整错误信息和堆栈跟踪
- 能否稳定复现？如果不能 → 收集更多数据，不要猜
- 检查最近的代码变更（git diff、近期提交）
- 多组件系统：在每个组件边界添加诊断日志，先收集证据，再分析

### Phase 2：模式分析
- 找到代码库中类似的、正常工作的代码
- 列出所有差异，无论多小都不要忽略

### Phase 3：假设与测试
- 明确陈述假设："我认为 X 是根本原因，因为 Y"
- 做最小改动验证假设（一次只改一个变量）
- 验证后再继续，不要叠加多个修复

### Phase 4：实施修复
- 先写能复现 bug 的失败测试
- 实施单一修复（解决根本原因，不是症状）
- 验证修复有效，所有测试通过

**如果已尝试 3 次以上修复仍失败 → 停下来，质疑架构设计，与用户讨论**

## 危险信号（立即停止，回到 Phase 1）

- "先快速修一下，之后再调查"
- "试试改 X 看会怎样"
- "我不完全理解，但这个可能有用"
- 在追踪数据流之前就提出解决方案
- 已经失败 2+ 次仍继续尝试同类修复
